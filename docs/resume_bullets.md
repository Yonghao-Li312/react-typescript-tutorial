# Resume Bullets

- Built a React and TypeScript task manager with active/completed columns, inline editing, deletion, completion toggles, and drag-and-drop movement.
- Managed typed component state across reusable `InputField`, `TodoList`, and `SingleTodo` components using React hooks and TypeScript interfaces.
- Improved UI accessibility for icon-only controls with labels, titles, and predictable button behavior.
